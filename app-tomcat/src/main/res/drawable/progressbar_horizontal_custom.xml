<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="2dp" />
            <gradient
                android:startColor="@color/colorContentBackground"
                android:centerColor="@color/colorContentBackground"
                android:centerY="0.75"
                android:endColor="@color/colorContentBackground"
                android:angle="270"
                />
        </shape>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="2dp" />
                <gradient
                    android:startColor="@color/fontColorRed"
                    android:centerColor="@color/fontColorRed"
                    android:centerY="0.75"
                    android:endColor="@color/fontColorRed"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="2dp" />
                <gradient
                    android:startColor="@color/fontColorRed"
                    android:centerColor="@color/fontColorRed"
                    android:centerY="0.75"
                    android:endColor="@color/fontColorRed"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>

</layer-list>